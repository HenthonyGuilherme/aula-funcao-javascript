// 1º)
function novoArray(){
    var nArray = ['Acre', 'Amapá', 'Amazonas'];
    return nArray.reverse();
  }
  
  // 2º)
  function novaString(string1, string2){
    var str1 = string1.toUpperCase();
    var str2 = string2.toUpperCase();
    var cont = string1 +' '+ string2;
    return cont;
  }
  
  // 3º)
  function repeticao(){
    var palavra = 'Repetição '
    var rept = palavra.repeat(17);
    return rept;
    
  }